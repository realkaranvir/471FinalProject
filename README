Demonstration of handling a mesh with no normals included in default execution is the red sphere on the right side of the view from icoNoNormals.obj.

Camera controls use scrolling. So if on a laptop, just use 2 fingers to move the camera around. Then wasd to move the camera in space.
